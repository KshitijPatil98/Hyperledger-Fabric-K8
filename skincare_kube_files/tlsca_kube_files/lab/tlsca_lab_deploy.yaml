apiVersion: apps/v1
kind: Deployment
metadata:
  name: tlsca-lab
  namespace: skincare
spec:
  selector:
    matchLabels:
      app: tlsca-lab
  replicas: 1
  template:
    metadata:
      labels:
        app: tlsca-lab
    spec:
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: skincarepvc
      containers:
        - name: tlsca-lab
          image: hyperledger/fabric-ca:1.5.7
          imagePullPolicy: "IfNotPresent"
          command:
            ["fabric-ca-server", "start", "-b", "tlscaadmin:tlscaadminpw", "-d"]
          resources:
            requests:
              memory: "300Mi"
              cpu: "250m"
            limits:
              memory: "400Mi"
              cpu: "350m"
          env:
            - name: FABRIC_CA_HOME
              value: /etc/hyperledger/fabric-ca-server
            - name: FABRIC_CA_SERVER_CA_NAME
              value: tlsca-lab
            - name: FABRIC_CA_SERVER_TLS_ENABLED
              value: "true"
            - name: FABRIC_CA_SERVER_PORT
              value: "7054"
          volumeMounts:
            - mountPath: /etc/hyperledger/fabric-ca-server
              subPath: organizations/tlsca_certs/lab
              name: data
